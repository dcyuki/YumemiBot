const { getConfig } = require(`${__yumemi}/utils/util`);

class Setu {
  static max = 50;
  static lsp = new Map();
  static path = `${__yumemi}/data/images/setu`;
  static api = tools.getConfig('api').lolicon;

  
}