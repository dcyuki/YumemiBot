// const sqlite3 = require('sqlite3').verbose();
// const { getConfig } = require(`${__yumemi}/utils/util`);
// const db = new sqlite3.Database(`${__yumemi}/data/db/yumemi.db`);

// class Battle {
//   constructor(ctx) {
//     const { group_id, group_name, user_id, raw_message, nickname, card, level } = ctx;

//     this.group_id = group_id;
//     this.group_name = group_name;
//     this.user_id = user_id;
//     this.raw_message = raw_message;
//     this.nickname = nickname;
//     this.card = card;
//     this.level = level;
//   }

//   // test() {
//   //   this.version = await getConfig('groups').then(data => data[this.group_id].plugins.battle.version);
//   // }
  
// }

// module.exports = Battle;